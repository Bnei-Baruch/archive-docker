# Archive site backend cron tasks

# full snapshot backup of all indices for suitcase sync
0 0 * * * root <INSTALL_DIR>/backup_elastic.sh

# Sanity check that search works properly.
0 * * * * root docker-compose -f <INSTALL_DIR>/docker-compose.yml exec archive_backend ./search_sanity_docker.sh

# Daily eval of prod
0 0 * * * root docker-compose -f <INSTALL_DIR>/docker-compose.yml exec archive_backend ./eval_docker.sh

# sync wordpress cms to disk
*/10 * * * * root docker-compose -f <INSTALL_DIR>/docker-compose.yml exec archive_backend ./cms_sync_docker.sh
