* imaginary can no longer point to nginx via 'localhost', we need to change the front-end to use 'nginx' instead
* logging
* volumes layout on host lvm
* nginx.conf set_real_ip_from
* cron jobs (search, cms)
* docker restarts ?!
* make sure email sending is working in production ! (once postfix is setup on the host)



* front-ends dynamic configs